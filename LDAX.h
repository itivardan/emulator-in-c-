#include<iostream>
#include<stdio.h>
#include<string>
#include<stdlib.h>
#include"dectohex.h"
using namespace std;
void HashEntry::ldax(string line)
{			
				string str1="",str2="";
				operation();
				char ch;
				int val,val1,no;
				unsigned short ptr,*ptr1;
	    		ch=line[5];
				if(ch=='B'){(ptr)=B;dectohex(ptr);}//ptr1=C;	val=ptr;val1=*ptr1;no=val*100+val1;A=memory[no];}
			
				cout<<ptr<<endl;
				pc+=2;
				
			
}

